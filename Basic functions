details to dataframe
pd.DataFrame(np.concatenate(df_details['details']).tolist())






Equality between two dataframes: df1.equals(df2)

with open('5k.json',encoding="utf-8") as j:
     contents = json.loads(j.read())


json to data frame : 
from pandas import json_normalize 
arr=[]
for i in df:
    for j in i:
        arr.append(j)  
df=json_normalize(arr)
